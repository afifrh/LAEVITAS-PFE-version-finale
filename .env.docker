# Fichier d'environnement pour Docker Compose
# Copiez ce fichier vers .env et modifiez les valeurs selon vos besoins

# Environnement
NODE_ENV=production

# Base de données MongoDB
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=password123
MONGO_DB_NAME=laevitas

# JWT Secret (CHANGEZ EN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production-with-strong-random-string

# API Binance (optionnel pour les tests)
BINANCE_API_KEY=your-binance-api-key
BINANCE_SECRET_KEY=your-binance-secret-key

# Ports (optionnel, utilise les valeurs par défaut si non spécifié)
FRONTEND_PORT=80
BACKEND_PORT=5000
MONGODB_PORT=27017
REDIS_PORT=6379